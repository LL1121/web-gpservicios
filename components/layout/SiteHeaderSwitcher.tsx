'use client'

import HeaderNavbarProposalA from '@/components/layout/header/HeaderNavbarProposalA'

/** Navbar unificado en todo el sitio (layout ondas + zona roja). */
export default function SiteHeaderSwitcher() {
  return <HeaderNavbarProposalA />
}
